# ══════════════════════════════════════════════════════════════════════════════
# requirements.txt
# Projet : Modélisation ML de l'Humidité du Sol — Conditions Semi-Arides
# Python >= 3.10
#
# Installation : pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════════════════════

# ── Manipulation de données ───────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0          # lecture fichiers Excel (.xlsx) — MCDI data

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Machine Learning classique ────────────────────────────────────────────────
scikit-learn>=1.3.0       # Ridge, RandomForest, SimpleImputer, StandardScaler, métriques, CV

# ── Gradient Boosting ─────────────────────────────────────────────────────────
xgboost>=2.0.0            # XGBoost (tree_method='hist' pour CPU)

# ── Deep Learning ─────────────────────────────────────────────────────────────
tensorflow>=2.13.0        # LSTM, GRU — CPU suffit pour ce dataset (~1000 obs)
# Alternative si TensorFlow pose problème sur votre OS :
# torch>=2.0.0            # PyTorch (remplacer le code LSTM en conséquence)

# ── Explicabilité ─────────────────────────────────────────────────────────────
shap>=0.43.0              # TreeExplainer pour RF et XGBoost

# ── Statistiques ──────────────────────────────────────────────────────────────
scipy>=1.11.0             # probplot (QQ-plot), stats

# ── Utilitaires ───────────────────────────────────────────────────────────────
tqdm>=4.65.0              # barres de progression (optionnel)
